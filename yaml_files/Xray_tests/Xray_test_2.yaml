#======== Input parameter declarations =======
Parameters:

 # model parameters for "DecayingDM_general"
 DecayingDM_photon:
   # mass in GeV
   mass:
     fixed_value: 1e-4

   # lifetime in s
   lifetime:
     range: [1e14, 1e30]
     prior_type: log

   # (initial) fraction of decaying species to all of omega_cdm
   fraction:
     range: [1e-15, 1e0]
     prior_type: log

#======== Prior setup ========
Priors:

#======== Output setup ========
Printer:
  printer: hdf5
  options:
    output_file: "ALP_test_2.hdf5"
    group: "/data"
    delete_fiel_on_restart: false
    buffer_length: 100

#======== Scanner setup ========
Scanner:
  use_scanner: square_grid

  scanners:
    square_grid:
      plugin: square_grid
      like: LogLike
      grid_pts: 500

#======== Observables of interest ========
ObsLikes:

  - purpose:      LogLike
    capability:   lnL_Xray_Integral_SPI_sterile_nu
    module:       DarkBit
    type:         double

  - purpose:      LogLike
    capability:   lnL_Xray_M31_sterile_nu
    module:       DarkBit
    type:         double

  - purpose:      LogLike
    capability:   lnL_Xray_NuSTAR_sterile_nu
    module:       DarkBit
    type:         double

#======== Rule entries ========
Rules:

#======== Logging setup ========
Logger:
  redirection:
    [Default]      : "default.log"

#======== Generic Name/Value Section ========
KeyValues:

  debug: false

  default_output_path: "runs/Xray_test_2"

  likelihood:
    model_invalid_for_lnlike_below: -1e6
