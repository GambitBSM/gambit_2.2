Parameters:
  GeneralALP: !import include/no_alp.yaml

Printer:
  printer: ascii
  options:
    output_file: "r.txt"

Scanner:
  use_scanner: random
  scanners:
    random:
      plugin: random
      point_number: 1
      like:  LogLike

ObsLikes:
  - capability: lnL_RParameter
    purpose: LogLike

KeyValues:

  default_output_path: "./runs/r"

  debug: false
