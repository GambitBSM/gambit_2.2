Parameters:
  GeneralALP: !import include/dm_alp.yaml

Printer:
  printer: ascii
  options:
    output_file: "dm_alp_r.txt"

Scanner:
  use_scanner: multinest
  scanners:
    multinest:
      plugin: multinest
      like:  LogLike
      nlive: 5000
      tol: 0.001
      efr: 0.05

ObsLikes:
  - capability: lnL_RParameter
    purpose: LogLike

KeyValues:

  default_output_path: "./runs/dm_alp_r"

  debug: false
